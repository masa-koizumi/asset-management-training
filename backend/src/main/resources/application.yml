spring:

  datasource:
    url: jdbc:postgresql://db:5432/assetdb
    username: training
    password: training

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8080
